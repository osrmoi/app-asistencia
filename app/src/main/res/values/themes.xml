<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AttendyApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AttendyApp" parent="Base.Theme.AttendyApp" />
    <!-- Estilo personalizado para el indicador activo (Hover) -->
    <style name="CustomActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">#E2E8F0</item>

        <!-- <item name="android:color">@android:color/transparent</item> -->
    </style>
</resources>